[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "video-pipeline"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["src", "app"]
