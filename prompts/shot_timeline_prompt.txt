You are the director breaking ONE finished scene into the separate shots that
will actually be rendered. Return ONLY JSON, no markdown fences, no preamble.

HOW THIS SCENE GETS MADE: every timed window in the scene -- (0-3s), (3-7s) and
so on -- is rendered as its OWN clip, one after another, by a video model that
has never seen the other clips. Nothing carries over between them except what
you write down here and the reference photographs you attach. So you are the
memory of the scene: the same person must have the same face, the same
clothes, the same props in the same hands, in the same light, from shot to
shot, and the only way that happens is if every shot is told so.

THE SCENE ({seconds} seconds in total):
{scene}

ITS TIMED WINDOWS, in order (one shot each -- keep these exact windows):
{windows}

REFERENCE PHOTOGRAPHS attached to the scene, numbered. The photographs
themselves are shown above this text, each under its number:
{references}

Write:

1. "continuity": the scene's memory, written once. Who is in it and exactly
   what they look like and wear; the props and their state; the location(s);
   light, time of day, grade, lens and texture; the avoid-list. Concrete nouns,
   no adjectives about quality. This block is placed IN FRONT OF every shot's
   prompt by code, so do not repeat it inside the shots.

2. "shots": one entry per timed window, same order, same windows. Each has:
   - "window": the window exactly as given, e.g. "0-3s".
   - "prompt": a paste-ready prompt for THIS shot only. One camera setup, one
     clear action that fits inside the window, camera move and framing, what
     is lit and how, and the diegetic sound you would hear. Start mid-motion.
     Say where this shot sits against the one before it ("hard cut to", "same
     angle, later", "reverse on") so the edit reads, but never describe what
     happens in other windows.
   - "refs": the numbers of the reference photographs THIS shot needs --
     the face of anyone visible, the prop in frame, the location it is in.
     Only what is actually on screen in this shot; a shot of an empty hallway
     does not need a portrait, and a close-up of hands does not need the room.
     Use the numbers above, never URLs or descriptions. [] when nothing
     applies.

Return exactly this JSON shape:
{"continuity": "...", "shots": [{"window": "0-3s", "prompt": "...", "refs": [1]}]}
