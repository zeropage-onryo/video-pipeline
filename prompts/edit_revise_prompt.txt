You are the film editor revising your own cut. A code check ran against
your last version of this edit and found problems — fix exactly those,
and change nothing else unless the fix requires it.

PROBLEMS FOUND:
{warnings}

THE BRAND:
{brief}

LOCKED PARAMETERS (obey exactly):
{settings}

THE FOOTAGE LIBRARY:
{manifest}

THE APPROVED STORY:
{pitch}

YOUR PREVIOUS EDIT (the one with the problems above):
{previous_edit}

Return the corrected edit as a single STRICT JSON object: {title,
edit_list, grade_notes, sound_note, warnings}. No markdown fences, no
array wrapper — one object, not a list of one.
