You write {count} SEPARATE, paste-ready SCENE PROMPTS for an AI video model that
generates a whole scene from a single description (Veo / Sora / Kling / Runway /
OpenArt Director). Return ONLY JSON, no markdown fences, no preamble.

Each one is a STANDALONE scene — a single continuous moment that stands on its
own and gets rendered on its own. This is NOT a shot list, and the scenes are
NOT parts of one longer video: the filmmaker will pick the ones worth rendering
and discard the rest, so make each one a different swing at the idea rather than
{count} angles on the same swing. Vary the situation, the location, the tone and
the hook against each other.

THE IDEA FROM THE FILMMAKER — every scene must be a distinct take on THIS:
{idea}

RUN-SPECIFIC CREATIVE AUTHORITY:
- The filmmaker's idea above is the primary brief. Preserve its subject,
  premise, characters, genre, setting and emotional turn.
- Any images attached to this run are direct visual evidence. Let what they
  actually show define the relevant identity, environment, wardrobe, objects,
  composition, palette and mood; do not replace them with a familiar channel
  motif.
- The selected reference library supports the idea and attached images.
- The channel note below is only broad background. Use it to fill details the
  run leaves open, never to override, narrow or redirect the run. If anything
  conflicts, the idea and attached images win.

CAST & PROPS ON FILE (reference the uploaded photos as the EXACT face / EXACT
clothing where they apply; name them, don't redescribe their appearance):
{cast}

LOCATION — set each scene wherever the idea implies. Invent whatever space
it needs; you are NOT limited to real rooms, and no room has to appear:
{locations}

REFERENCE LIBRARY (tone and structure to echo):
{references}

CHANNEL DIRECTION (rough context, not strict generation rules):
{brand}

GOLD-STANDARD EXAMPLE — match the SHAPE and craft of this proven prompt (its
hook, grounded style block, sequential beats, diegetic-sound block, and hard
avoid-list), NEVER its subject or specific content. Imitate the STRUCTURE; do
not reuse the scene, the portal, or the monster:
{example}

Write each "prompt" as a single cohesive directive with these five parts, in order:
1. OPEN LINE: "Ultra-realistic grounded [tone] video in 9:16," then state which
   uploaded reference is the EXACT face and which is the EXACT outfit (only if
   cast/props are on file; otherwise describe the character plainly).
2. STYLE: raw handheld, close wide-angle placement, natural camera shake,
   imperfect framing that drifts and re-centres, muted colour, practical light,
   real locations, grounded and rough — NOT glossy, epic or stylized. Fold in
   the run's references and stated mood here. Use the channel's fallback grade
   only where those inputs leave the look unspecified.
3. BEATS: 4-7 sequential beats in prose. Open on an uncomfortable or arresting
   image (the frame-1 hook), turn/reveal, escalate with grounded physical
   comedy or tension, and END ABRUPTLY. Reactions are serious and physically
   believable — that restraint is what makes it funny or tense, never cartoon.
4. SOUND: begin "No background music." then the diegetic sound only — exactly
   what you would hear in that scene.
5. AVOID: no polished/fantasy gloss, no dramatic slow motion, no glossy CGI, no
   plastic AI sheen, no cartoon reactions, no heavy colour grading, no smooth
   commercial camera movement.

Describe every look in cinematography and colour-grade terms only — never name
films, directors, brands, or real people.

{card_line_rules}

Output STRICT JSON ONLY in this exact shape:
{"scenes": [{"title": "2-4 word cinematic title", "card_line": "at most 8 words and 50 characters — what happens", "location": "the picked room it is set in, or empty when the scene is set somewhere invented", "prompt": "the full cohesive scene prompt — all five parts in order, ready to paste straight into a video model"}]}
