# whisper/torch disabled 2026-08-19: no code in app/ or src/ imports whisper or numpy (transcription was never wired up), and torch has no pip wheel for Intel macOS + Python 3.13. Re-enable only after building on a Python <=3.12 venv, or once torch resumes shipping Intel Mac wheels.
# openai-whisper>=20250625   # pin off the old 20240930 release -- its legacy setup.py imports pkg_resources directly, which current setuptools no longer bundles, breaking the whole batch install
# numpy<2   # whisper's torch build fails against numpy 2.x (torch._C._ARRAY_API); unpinning breaks transcription
google-genai
python-dotenv
requests
boto3   # R2 speaks the S3 API; storage.py's client, no Cloudflare-specific SDK needed
runwayml   # runway.py's client -- API credit spend, gated behind RUNWAY_SPEND_OK=1
pytest
ruff
fastapi
uvicorn
jinja2
python-multipart
Pillow
pillow-heif   # .heic decoding -- iPhone photos are half the asset bank, and without this a HEIC reference is accepted, listed, and then silently dropped at render
psycopg[binary]
pgvector
pypdf   # /library/ingest's PDF file upload -- text extraction before chunking
langgraph
langsmith
mcp>=2   # the MCP surface (src/mcp_server.py). v2 renamed FastMCP to MCPServer and moved stateless_http onto streamable_http_app(); code written against 1.x imports a module that no longer exists, so this pin is load-bearing -- verify src/mcp_server.build_server on a major bump
authlib
itsdangerous
argon2-cffi
